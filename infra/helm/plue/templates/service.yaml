apiVersion: v1
kind: Service
metadata:
  name: {{ include "plue.fullname" . }}
  labels:
    {{- include "plue.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plue.selectorLabels" . | nindent 4 }}
