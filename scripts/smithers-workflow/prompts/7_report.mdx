REPORTING — Ticket: {props.ticketId} — {props.ticketTitle}

Generate concise, easy-to-read report of ticket implementation.

IMPLEMENTATION SUMMARY:
{props.whatWasDone}

FILES CHANGED:
Created: {JSON.stringify(props.filesCreated)}
Modified: {JSON.stringify(props.filesModified)}

COMMITS:
{JSON.stringify(props.commitMessages)}

TEST RESULTS:
- All tests passing: {props.allTestsPassing}
{props.failingSummary ? `- Failures: ${props.failingSummary}` : ""}

REVIEW ROUNDS: {props.reviewRounds}
{props.reviewIssues ? `Review issues encountered:\n${props.reviewIssues}` : "No review issues."}

PRE-COMPUTED METRICS (echo these back exactly in your output JSON):
- filesChanged: {props.filesChanged}
- testsAdded: {props.testsAdded}
- reviewRounds: {props.reviewRounds}

Assess:
- Anything go wrong?
- Agent struggle with anything?
- Lessons for future tickets?

**REQUIRED OUTPUT** — Must end with JSON matching schema:
{props.reportSchema}
Use the pre-computed values above for filesChanged, testsAdded, reviewRounds.
Only generate: summary, status, struggles, timeSpent, lessonsLearned.
Don't skip JSON output. Workflow fails without it.
