import SwiftUI

struct IDEWindowRootView: View {
    var body: some View {
        VStack(spacing: 0) {
            Text("IDE")
                .font(.system(size: DS.Typography.base))
                .padding(DS.Space._16)
            Spacer()
        }
    }
}
