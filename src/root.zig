// Root module for the Zig library
// Main functionality is now in libplue.zig and farcaster.zig
const std = @import("std");

fn main() void {
    std.log.warn("Warning: plue zig code is currently just a lib. The app is built with swift");
}
