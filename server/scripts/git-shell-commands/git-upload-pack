#!/usr/bin/env bash
# Plue Git Upload Pack (Clone/Fetch) Handler
# Integrates with Plue authentication

set -euo pipefail

# Get repository path from argument
REPO_PATH="$1"

# Execute git-upload-pack
exec /usr/bin/git-upload-pack "$REPO_PATH"
