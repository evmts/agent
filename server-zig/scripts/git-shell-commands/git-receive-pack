#!/usr/bin/env bash
# Plue Git Receive Pack (Push) Handler
# Integrates with Plue authentication and triggers jj sync

set -euo pipefail

# Get repository path from argument
REPO_PATH="$1"

# Execute git-receive-pack
exec /usr/bin/git-receive-pack "$REPO_PATH"
